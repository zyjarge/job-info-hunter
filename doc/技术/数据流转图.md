graph TD
    %% 展示层
    subgraph Presentation_Layer[展示层]
        User[用户] --> |1. 设置查询条件| Frontend[前端界面]
    end

    %% 接入层
    subgraph Gateway_Layer[接入层]
        Frontend --> |2. 发送查询请求| Gateway[API网关]
        Gateway --> |3. 路由请求| JobService[职位服务]
    end

    %% 数据存储层
    subgraph Storage_Layer[数据存储层]
        MySQL[(MySQL)]
        Redis[(Redis缓存)]
        ES[(Elasticsearch)]
    end

    %% 应用服务层
    subgraph Application_Layer[应用服务层]
        JobService --> |4. 查询缓存| Storage_Layer
        JobService --> |5. 查询数据| Storage_Layer
        
        Scheduler[任务调度器] --> |6. 触发爬虫任务| CrawlerService[爬虫服务]
        NotificationService[推送服务]
    end

    %% 任务分发层
    subgraph Message_Layer[消息层]
        CrawlerService --> |7. 分发任务| RabbitMQ{RabbitMQ}
    end

    %% 数据采集层
    subgraph Crawler_Layer[数据采集层]
        RabbitMQ --> |8. 消费任务| Crawler1[BOSS直聘爬虫]
        RabbitMQ --> |8. 消费任务| Crawler2[智联招聘爬虫]
        RabbitMQ --> |8. 消费任务| Crawler3[拉勾网爬虫]
        RabbitMQ --> |8. 消费任务| Crawler4[猎聘网爬虫]
    end

    %% 数据处理层
    subgraph Data_Processing_Layer[数据处理层]
        Crawler1 & Crawler2 & Crawler3 & Crawler4 --> |9. 原始数据| DataProcessor[数据处理服务]
        DataProcessor --> |10. 清洗后数据| MySQL
        DataProcessor --> |11. 同步数据| ES
        DataProcessor --> |12. 更新缓存| Redis
    end

    %% 数据应用层
    subgraph Data_Application_Layer[数据应用层]
        MySQL --> |13. 数据分析| AnalysisService[数据分析服务]
        AnalysisService --> |14. 分析结果| ReportService[报表服务]
    end

    %% 监控层
    subgraph Monitor_Layer[监控层]
        Prometheus[监控系统] --> |17. 采集指标| CrawlerService & DataProcessor
        Prometheus --> |18. 告警| AlertManager[告警管理器]
        AlertManager --> |19. 发送告警| Admin[管理员]
    end

    %% 数据流转
    DataProcessor --> |15. 新职位通知| NotificationService
    NotificationService --> |16. 发送通知| User

    %% 样式定义
    classDef layer fill:#f0f0f0,stroke:#333,stroke-width:2px;
    classDef service fill:#f9f,stroke:#333,stroke-width:2px;
    classDef database fill:#fff,stroke:#333,stroke-width:2px;
    classDef queue fill:#fcf,stroke:#333,stroke-width:2px;

    %% 应用样式
    class Presentation_Layer,Gateway_Layer,Application_Layer,Message_Layer,Crawler_Layer,Data_Processing_Layer,Storage_Layer,Data_Application_Layer,Monitor_Layer layer;
    class JobService,CrawlerService,DataProcessor,AnalysisService,ReportService,NotificationService service;
    class MySQL,Redis,ES database;
    class RabbitMQ queue;